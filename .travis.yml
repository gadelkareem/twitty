language: php

cache:
  directories:
    - $HOME/.composer/cache/files

php:
  - 7.2

matrix:
  fast_finish: true

before_install:
  - phpenv config-rm xdebug.ini
  - composer install --no-interaction

script: ./bin/phpunit



